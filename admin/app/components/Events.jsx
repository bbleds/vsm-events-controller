'use strict';

const React = require('react');

const Events = React.createClass({
  // render this to DOM
  render: function(){
    return (
      <div>
        <h1>Event list should display here</h1>
      </div>
    );
  }
});

module.exports = Events;
