'use strict';

const React = require('react');

const SignUpList = React.createClass({
  // render this to DOM
  render: function(){
    return (
      <div>
        <h1>Sign ups should display here</h1>
      </div>
    );
  }
});

module.exports = SignUpList;
